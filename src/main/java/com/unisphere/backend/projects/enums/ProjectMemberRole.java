package com.unisphere.backend.projects.enums;

/** Authorization role on a project's team roster — not the same as the "position" a member fills. */
public enum ProjectMemberRole {
    OWNER,
    CONTRIBUTOR
}
